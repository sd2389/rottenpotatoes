<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div class="alert alert-danger">
      <h5><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h5>
      <ul>
        <% @movie.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.text_field :rating %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :release_date %><br>
    <%= f.datetime_select :release_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

